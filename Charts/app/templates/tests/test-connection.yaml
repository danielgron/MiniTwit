apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "institutions-api.fullname" . }}-test-connection"
  labels:
    {{- include "institutions-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "institutions-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
